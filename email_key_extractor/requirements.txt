# Dependencias del extractor de claves desde correo

# Microsoft Graph API (Outlook)
msal==1.31.1

# HTTP
requests==2.32.3

# Cifrado local (vault Fernet)
cryptography==43.0.3

# Variables de entorno
python-dotenv==1.0.1

# Azure Key Vault (opcional – instalar solo si se usa AzureVault)
# azure-identity==1.19.0
# azure-keyvault-secrets==4.9.0
